services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: runner
    container_name: plameli-frontend-prod
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
